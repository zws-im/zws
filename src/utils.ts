export enum Env {
	Prod = 'production',
	Dev = 'development',
}

export enum OpenApiTags {
	Urls = 'urls',
	Stats = 'stats',
	Shields = 'shields',
	Health = 'health',
}

export enum OpenApiSecuritySchemes {
	ApiKey = 'apiKey',
}

export enum SentryBreadcrumbCategory {
	Database = 'db',
	Request = 'request',
}
