{
	"$schema": "https://json.schemastore.org/lerna.json",
	"command": {
		"publish": {
			"allowBranch": "main",
			"message": "build: publish"
		},
		"version": {
			"allowBranch": "main",
			"message": "build: bump version"
		}
	},
	"npmClient": "yarn",
	"packages": ["packages/*"],
	"useWorkspaces": true,
	"version": "independent"
}
