{
	"$schema": "https://json.schemastore.org/lerna.json",
	"command": {
		"version": {
			"allowBranch": "main",
			"message": "build: bump version"
		},
		"publish": {
			"allowBranch": "main",
			"message": "build: publish"
		}
	},
	"npmClient": "yarn",
	"packages": ["packages/*"],
	"version": "independent"
}
